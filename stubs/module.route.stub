package route

import (
	"ecommerce/internal/handler"

	"github.com/gofiber/fiber/v2"
)

func Setup{{ .ModuleTitle }}Routes(app *fiber.App, {{ .ModuleCamelTitle }}Handler handler.{{ .ModuleTitle }}Handler) {
	route := app.Group("/{{ .ModuleVar }}")

	route.Get("/", {{ .ModuleCamelTitle }}Handler.Get{{ .ModuleTitle }}s)
	route.Get("/:id", {{ .ModuleCamelTitle }}Handler.Get{{ .ModuleTitle }}ByID)
	route.Post("/", {{ .ModuleCamelTitle }}Handler.Create{{ .ModuleTitle }})
	route.Put("/:id", {{ .ModuleCamelTitle }}Handler.Update{{ .ModuleTitle }})
	route.Delete("/:id", {{ .ModuleCamelTitle }}Handler.Delete{{ .ModuleTitle }})
}
